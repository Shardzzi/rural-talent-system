## [2026-04-01] F4 Scope Fidelity Problems
- T10: backend stats are real, but the admin UI still hardcodes values and omits requested distribution details.
- T12: validation is improved, but real-time feedback and full age-range parity are not fully met.
- T13: export still depends on fallback behavior because the detailed query/schema mismatch is unresolved.
- T15-T17: test coverage exists, but several error/edge/search/auth scenarios remain unverified.
